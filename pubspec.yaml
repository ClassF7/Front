name: flutter_t3t4
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mobx: ^2.0.5
  flutter_mobx: ^2.0.2
  http: ^0.13.4
  flutter_rating_bar: ^4.0.0
  getwidget: ^2.0.4
  
dev_dependencies:
  build_runner: ^2.0.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mobx_codegen: ^2.0.4
flutter:
  uses-material-design: true
  assets:
    - assets/images/projfilmesbanner.png

scripts:
  mobx_build: flutter pub run build_runner build
  mobx_watch: flutter pub run build_runner watch --delete-conflicting-outputs
